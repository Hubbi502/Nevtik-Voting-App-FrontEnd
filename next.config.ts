import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['localhost', "i.pinimg.com", "cdn140.picsart.com", "nevtik-voting-app-backend-production.up.railway.app"],
  },
};

export default nextConfig;
